"""These are all the precalculated values for tests.

Once we got a manually proven correct solution, we can use it as "expected" values to check our function against.
Obviously, we mostly just to approximate comparisons as rounding errors are inevitable. We haven't check whether the
inputs are well conditioned either but that's fine.

Each file in this module corresponds to the python file with the same name.
"""
